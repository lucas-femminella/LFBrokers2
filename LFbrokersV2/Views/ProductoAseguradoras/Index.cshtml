@model IEnumerable<LFbrokersV2.Models.ProductoAseguradora>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Activo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ComisionPrimaBase)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AseguradoraNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Activo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ComisionPrimaBase)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AseguradoraNavigation.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProductoNavigation.Producto1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
