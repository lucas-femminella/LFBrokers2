@model IEnumerable<LFbrokersV2.Models.CodigoPostal>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CodigoPostal1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LocalidadNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ZonaNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CodigoPostal1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LocalidadNavigation.Localidad1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ZonaNavigation.Zona1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
